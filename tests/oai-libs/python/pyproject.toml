[project]
name = "python"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "openai>=1.64.0",
    "pytest>=7.0.0",
    "requests-mock>=1.10.0",
    "responses>=0.23.0",
]
